type Landfills @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  address: String
  city: String
  state: String
  zip: String
  country: String
  lat: Float
  lng: Float
  active: Boolean
}